package daguito

// Version is the SDK version exposed in X-Daguito-Client-Version headers.
// Override with -ldflags="-X github.com/daguitocontact-lang/go-sdk.Version=v0.3.1"
// at build time when releasing; the in-repo default tracks the latest tag.
var Version = "0.5.5"

const sdkLang = "go"
